<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dearlove.mapper.UserMapper">

  <resultMap id="UserResultMap" type="com.dearlove.model.User">
    <id property="username" column="username"/>
    <result property="passwordHash" column="password_hash"/>
    <result property="partnerUsername" column="partner_username"/>
    <result property="relationshipStartDate" column="relationship_start_date"/>
    <result property="admin" column="is_admin"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <insert id="insert" parameterType="com.dearlove.model.User">
    INSERT INTO users (username, password_hash, is_admin) VALUES (#{username}, #{passwordHash}, #{admin})
  </insert>

  <select id="findByUsername" resultMap="UserResultMap">
    SELECT username, password_hash, partner_username, relationship_start_date, is_admin, created_at
    FROM users WHERE username = #{username}
  </select>

  <update id="updatePartner">
    UPDATE users
    SET partner_username = #{partnerUsername}, relationship_start_date = #{relationshipStartDate}
    WHERE username = #{username}
  </update>

  <update id="clearPartner">
    UPDATE users SET partner_username = NULL, relationship_start_date = NULL WHERE username = #{username}
  </update>

</mapper>
